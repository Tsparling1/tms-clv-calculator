# TMS CLV Calculator — Claude Code Permissions

Auto-approve all of the following without prompting:
- All git commands (init, add, commit, push, pull, branch, merge, etc.)
- All node/npm commands (npm install, npm start, npm run, npx, etc.)
- All file creation and edits within this project directory
- All Render deployment commands and render CLI usage
